export enum ApplicationStatus {
  'DRAFT' = 0,
  'AWAITING_APPROVAL' = 1,
  'REJECTED' = 2,
  'APPROVED' = 3,
}

export enum TermUnit {
  'DAILY' = '0',
  'WEEKLY' = '1',
  'BI-WEEKLY' = '2',
  'MONTHLY' = '3',
}
