import NoRecord from '@/components/no-records'

const Authentication = () => {
  return (
    <section className='card p-24px d-flex flex-column gap-30px h-100'>
      <h3 className='fs-20 fw-bold m-0'>Authentication</h3>

      <NoRecord text='Nothing show here' />
    </section>
  )
}

export default Authentication
